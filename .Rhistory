library(usethis)
proj_get()
use_mit_license("Alfredo G Marquez")
use_tidy_versions()
use_dev_package(c("random","tidyverse","reshape2"))
use_dev_package(c("random") #,"tidyverse","reshape2"))
)
use_package(c("random","tidyverse","reshape2"), type = "Import")
library(purrr)
map(pckg_list, use_package(type = "Import"))
pckg_list <- c("random","tidyverse","reshape2")
map(pckg_list, use_package(type = "Import"))
map(pckg_list, use_package(package = ., type = "Import"))
invoke_map(pckg_list, use_package(package = ., type = "Import"))
invoke_map(pckg_list, use_package( type = "Import"))
invoke_map(pckg_list, print) # use_package( type = "Import"))
invoke_map(print,pckg_list ) # use_package( type = "Import"))
invoke_map(use_package( type = "Import"),pckg_list ) # )
map_chr(use_package( type = "Import"),pckg_list )
use_package( "random", type = "Import")
use_package( "tidyverse", type = "Import")
use_package( "reshape2", type = "Import")
use_vignette("Group Selection")
use_readme_rmd()
install.packages("git2r")
use_readme_rmd()
